[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"

[project]
name = "gordian-orchestration"
version = "0.1.0"
description = "Thin experiment and process orchestration for the Rust Gordian substrate"
requires-python = ">=3.14"
license = "Apache-2.0"
dependencies = []

[project.optional-dependencies]
dev = ["ruff==0.16.5"]

[project.scripts]
gordian-project-sync = "gordian_orchestration.github_project:main"
gordian-derive-status = "gordian_orchestration.derive_status:main"
gordian-atom-registry = "gordian_orchestration.atom_registry:main"
gordian-bootstrap = "gordian_orchestration.bootstrap_claims:main"
gordian-milestone-contracts = "gordian_orchestration.milestone_contracts:main"

[tool.ruff]
target-version = "py314"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
