# Agent entrypoint

The canonical coding-agent instructions are in [`AGENTS.md`](AGENTS.md).

`AGENTS.md` is intentionally plural because that is the instruction filename consumed by current Codex tooling. This file exists as the human-facing singular entrypoint requested for Gordian.
